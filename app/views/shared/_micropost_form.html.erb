<%= simple_form_for(@micropost) do |f| %>
  <%= f.input :content, placeholder: "Compose new micropost..." %>
  <%= f.submit "Post", class: "btn btn-primary btn-block" %>
  <%= f.input :picture %>
<% end %>
